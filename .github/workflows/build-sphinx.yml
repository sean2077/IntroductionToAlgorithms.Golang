name: build-sphinx-to-gh-pages

on:
  push:
    branches: [main]

jobs:
  build_sphinx_job:
    runs-on: ubuntu-latest
    container: zhangxianbing/python-runtime:v0.0.1-3b4c92d

    steps:
      - name: Update docs
        run: |
          make github
        shell: bash
